{{-- [Component: shared/cta-get-started] "Get Started" CTA band used on multiple pages --}}
<section id="cta-get-started" class="py-16 px-6 bg-primary text-brand-black">
    <div class="max-w-7xl mx-auto flex flex-col gap-6 md:flex-row md:items-center md:justify-between">
        <div>
            <h2 class="font-heading text-3xl">{{-- CTA headline placeholder --}}</h2>
            <p class="font-body text-base text-brand-black/80">{{-- CTA support statement placeholder --}}</p>
        </div>
        <a href="{{ route('inquiry') }}" class="inline-flex items-center justify-center rounded-full bg-brand-black px-6 py-3 text-sm font-semibold text-brand-white">Start your inquiry</a>
    </div>
</section>
