{{-- [Component: contact/location-map] Contact page location and map placeholder --}}
<section id="contact-location-map" class="py-16 px-6 bg-brand-white">
    <div class="max-w-7xl mx-auto rounded-[2rem] bg-complementary p-10">
        <div class="grid gap-10 lg:grid-cols-[1.5fr_1fr]">
            <div class="space-y-6">
                <h2 class="font-heading text-3xl">{{-- Location headline placeholder --}}</h2>
                <p class="font-body text-brand-black/75">{{-- Location description placeholder --}}</p>
                <div class="grid gap-4 sm:grid-cols-2">
                    <div class="rounded-3xl bg-brand-white p-6 shadow-sm border border-brand-black/10">
                        <p class="font-heading text-sm uppercase tracking-[0.3em] text-brand-black/70">
                            {{-- Address label --}}</p>
                        <p class="font-body mt-3 text-brand-black/75">{{-- Address placeholder --}}</p>
                    </div>
                    <div class="rounded-3xl bg-brand-white p-6 shadow-sm border border-brand-black/10">
                        <p class="font-heading text-sm uppercase tracking-[0.3em] text-brand-black/70">
                            {{-- Contact label --}}</p>
                        <p class="font-body mt-3 text-brand-black/75">{{-- Contact details placeholder --}}</p>
                    </div>
                </div>
            </div>
            <div class="h-96 rounded-[2rem] bg-brand-black/5 border border-brand-black/10">
                {{-- Placeholder for embedded map or location illustration --}}
            </div>
        </div>
    </div>
</section>
